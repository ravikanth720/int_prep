package algorithms;

import java.util.Arrays;

public class FindMinDist {
    public static int findMinDistance(int w, int h, int n) {
        int[][] lot = new int[w][h];
        for (int i=0; i<h; i++) {
            for (int j = 0; j<w; j++) {
                lot[i][j] = Integer.MAX_VALUE;
            }

        }

        while(n >= 0) {
        }
        return 0;
    }
}
